# Test plan — expected values and behaviour against design intent

_Generated from `tests/plan/cases-numbers.js` and `tests/plan/cases-behaviour.js`._
_Do not edit this file: edit the cases and re-run `node tests/run-test-plan.js`._

Every case states three things: what the application is **meant** to do and why
(the design intent), what that means **numerically or behaviourally** (the
expectation), and where the expectation came from. The expectation is derived from
the fixture inputs by hand, not read out of a run — so you can disagree with the
expectation itself, which is the only way a test can catch an application that is
confidently and self-consistently wrong.

**Fixtures.** `qa-reference.json` is a five-activity plan whose inputs were chosen so
every derived number is a whole number checkable on paper. `crm-rollout.json` is a
real export, carrying the shape the sample never had: a baseline taken after the work
started, so activities finish before their own baseline windows open.

**Last run:** build 2026.08.02.1859 — **45 of 45 passing**.

---

## PERT estimation

### N1 — Expected duration is the PERT weighted mean  ✅ PASS

**Design intent.** A three-point estimate exists so the plan carries a range rather than a guess. TE = (O +
4M + P) / 6 weights the most likely outcome four times, which is what makes it a
Beta-PERT mean and lets the Monte Carlo agree with the deterministic pass.

**Expected.** `A Design` = 3 · `B Build` = 5 · `C Docs` = 2 · `D Test` = 4 · `E Sign-off` = 0

**Derived from.** (2+12+4)/6=3 · (4+20+6)/6=5 · (1+8+3)/6=2 · (1+16+7)/6=4 · milestone=0

**Actual.** `A Design` = 3 · `B Build` = 5 · `C Docs` = 2 · `D Test` = 4 · `E Sign-off` = 0

### N2 — Activity variance is the squared sixth of the range  ✅ PASS

**Design intent.** Variance drives the sensitivity ranking and the critical-path sigma. ((P - O) / 6)^2
treats the O-to-P span as roughly six standard deviations, which is the assumption the
whole three-point method rests on.

**Expected.** `A Design` = 0.1111111111111111 · `B Build` = 0.1111111111111111 · `C Docs` = 0.1111111111111111 · `D Test` = 1

**Derived from.** ((4-2)/6)^2 = 1/9 · ((6-4)/6)^2 = 1/9 · ((3-1)/6)^2 = 1/9 · ((7-1)/6)^2 = 1

**Actual.** `A Design` = 0.1111111111111111 · `B Build` = 0.1111111111111111 · `C Docs` = 0.1111111111111111 · `D Test` = 1

### B22 — The weighted mean is distinguishable from a plain average  ✅ PASS

**Design intent.** Every three-point estimate in the reference plan is symmetric — O and P sit the same
distance either side of M — and for a symmetric estimate EVERY weighted mean returns M.
So case N1 is satisfied by (O+4M+P)/6, by (O+M+P)/3, and by simply reading M. It cannot
fail, which means it was never testing anything. This case uses a SKEWED estimate, where
the four-times weight on the most likely outcome is the whole difference between the
Beta-PERT mean and a shrug. That weight is what makes the Monte Carlo mean land on the
deterministic finish; get it wrong and the two disagree with no visible symptom.

**Expected.** `te` = 3 · `plainAverageWouldSay` = 4 · `medianWouldSay` = 2 · `varianceFromTheRange` = 1.7777777777777777

**Derived from.** O=1 M=2 P=9. PERT (1 + 8 + 9)/6 = 3. Plain mean (1+2+9)/3 = 4. Variance ((9-1)/6)^2 = 16/9.

**Actual.** `te` = 3 · `plainAverageWouldSay` = 4 · `medianWouldSay` = 2 · `varianceFromTheRange` = 1.7777777777777777

### B23 — The editor previews the same estimate the plan will hold  ✅ PASS

**Design intent.** The activity editor computes a live TE from the fields as they are typed, before any of
it is in the plan. That is a second implementation of the load-bearing identity, and a
preview that disagrees with the save is worse than no preview — it is the number the
person actually decided on. Breaking the editor's copy alone left every other check in
this suite green, which is how this case came to exist.

**Expected.** `previewMatchesPert` = true · `previewMatchesWhatWasSaved` = true

**Derived from.** Type O=1 M=2 P=9 into the editor, read the live readout, save, read the activity.

**Actual.** `previewMatchesPert` = true · `previewMatchesWhatWasSaved` = true

## Critical path method

### N3 — Forward pass: each activity starts when its last predecessor finishes  ✅ PASS

**Design intent.** ES is the latest EF among predecessors. If the forward pass ever violates its own edges
the finish date is fiction and nothing downstream would notice.

**Expected.** `A Design` = "0→3" · `B Build` = "3→8" · `C Docs` = "3→5" · `D Test` = "8→12" · `E Sign-off` = "12→12"

**Derived from.** A has no predecessor. B and C both wait on A (EF 3). D waits on B (EF 8). E waits on D and C.

**Actual.** `A Design` = "0→3" · `B Build` = "3→8" · `C Docs` = "3→5" · `D Test` = "8→12" · `E Sign-off` = "12→12"

### N4 — Backward pass: each activity must finish before its earliest successor must start  ✅ PASS

**Design intent.** LF is the smallest successor LS, or the project finish where there is no successor. This
is what turns a set of durations into float.

**Expected.** `A Design` = "0→3" · `B Build` = "3→8" · `C Docs` = "10→12" · `D Test` = "8→12" · `E Sign-off` = "12→12"

**Derived from.** C's only successor is E at LS 12, so C may finish as late as 12 and start as late as 10.

**Actual.** `A Design` = "0→3" · `B Build` = "3→8" · `C Docs` = "10→12" · `D Test` = "8→12" · `E Sign-off` = "12→12"

### N5 — Total slack agrees whether measured from the start or the finish  ✅ PASS

**Design intent.** Slack must equal LS - ES AND LF - EF. If those two disagree the backward pass is
inconsistent with the forward pass and every float figure on the Gantt is wrong.

**Expected.** `A Design` = 0 · `B Build` = 0 · `C Docs` = 7 · `D Test` = 0 · `E Sign-off` = 0

**Derived from.** C: LS 10 - ES 3 = 7, and LF 12 - EF 5 = 7. The others are on the critical path.

**Actual.** `A Design` = 0 · `B Build` = 0 · `C Docs` = 7 · `D Test` = 0 · `E Sign-off` = 0

### N6 — An activity is critical exactly when it has no float  ✅ PASS

**Design intent.** Critical must be the same statement as zero slack, in both directions. A plan that marks
a slack-bearing activity critical over-constrains every decision made from it; one that
misses a zero-slack activity hides what actually moves the date.

**Expected.** `A Design` = true · `B Build` = true · `C Docs` = false · `D Test` = true · `E Sign-off` = true

**Derived from.** Only C carries float, so only C is not critical.

**Actual.** `A Design` = true · `B Build` = true · `C Docs` = false · `D Test` = true · `E Sign-off` = true

### N7 — The critical path is a continuous chain that reaches the finish  ✅ PASS

**Design intent.** It is presented as a PATH, so a reader traces it expecting each activity to feed the
next. A disconnected set of zero-slack activities displayed as a sequence is the classic
CPM presentation bug.

**Expected.** `0` = "A Design" · `1` = "B Build" · `2` = "D Test" · `3` = "E Sign-off"

**Derived from.** A(0-3) → B(3-8) → D(8-12) → E(12). No gaps, and it ends where the project ends.

**Actual.** `0` = "A Design" · `1` = "B Build" · `2` = "D Test" · `3` = "E Sign-off"

### N8 — Project duration is the largest earliest finish  ✅ PASS

**Design intent.** The plan is as long as its longest chain, not the sum of its parts.

**Expected.** `12`

**Derived from.** D finishes at 12; nothing finishes later.

**Actual.** `12`

## Working calendar

### N9 — Durations map onto working days, skipping weekends  ✅ PASS

**Design intent.** Effort is counted in working days and a commitment is made in calendar dates. The
translation must skip weekends or every date quoted to a client is early.

**Expected.** `A Design` = "2026-03-02→2026-03-04" · `B Build` = "2026-03-05→2026-03-11" · `C Docs` = "2026-03-05→2026-03-06" · `D Test` = "2026-03-12→2026-03-17"

**Derived from.** B occupies indices 3,4,5,6,7 = Thu 05, Fri 06, Mon 09, Tue 10, Wed 11 — the weekend is skipped.

**Actual.** `A Design` = "2026-03-02→2026-03-04" · `B Build` = "2026-03-05→2026-03-11" · `C Docs` = "2026-03-05→2026-03-06" · `D Test` = "2026-03-12→2026-03-17"

### N10 — A milestone is marked the working day after the work it follows  ✅ PASS

**Design intent.** DELIBERATE, and the choice is contestable — three independent derivations put this a day
earlier. A sign-off is an event somebody attends after the work is finished, not the
instant the work stops, so dating go-live on the same day the last build task ends would
promise a client something nobody does. Because it is a choice, the panel must SAY so —
see B17.

**Expected.** `"2026-03-18"`

**Derived from.** D finishes Tue 17 Mar; E is marked Wed 18 Mar. The plan spans 13 working days and holds 12.

**Actual.** `"2026-03-18"`

## Cost

### N11 — Activity cost is day rate times duration times allocation  ✅ PASS

**Design intent.** Cost is what delivery consumes. It is derived rather than typed so it cannot drift from
the plan it prices.

**Expected.** `A Design` = 3000 · `B Build` = 2500 · `C Docs` = 1000 · `D Test` = 4000 · `E Sign-off` = 0

**Derived from.** Alice $1,000 x 3 · Bob $500 x 5 · Bob $500 x 2 · Alice $1,000 x 4 · milestone $0

**Actual.** `A Design` = 3000 · `B Build` = 2500 · `C Docs` = 1000 · `D Test` = 4000 · `E Sign-off` = 0

### N12 — A client-side person costs nothing and bills nothing  ✅ PASS

**Design intent.** The client's own people appear in the plan because they consume calendar time and can be
double-booked — but they are not your cost and you do not invoice for them. Counting
them either way overstates the engagement.

**Expected.** `cost` = 4000 · `revenue` = 8000

**Derived from.** D is worked by Alice AND Cleo. Cleo is client-side, so D prices on Alice alone.

**Actual.** `cost` = 4000 · `revenue` = 8000

## Pricing

### N13 — Revenue is bill rate times duration, and the totals add up  ✅ PASS

**Design intent.** Revenue and cost are two different rates over the same effort. Keeping them separate is
what makes margin a real number rather than a mark-up assumption.

**Expected.** `cost` = 10500 · `revenue` = 21000

**Derived from.** cost 3,000+2,500+1,000+4,000 · revenue 6,000+5,000+2,000+8,000

**Actual.** `cost` = 10500 · `revenue` = 21000

### N14 — Margin is the share of the price that is not cost  ✅ PASS

**Design intent.** (price - cost) / price, computed on the price you will actually invoice. Computing it on
a not-to-exceed cap instead was a real defect in this panel once: it reported 54.8%
where the true figure was 37.7%.

**Expected.** `50`

**Derived from.** (21,000 - 10,500) / 21,000 = 10,500 / 21,000 = exactly one half

**Actual.** `50`

## Earned value

### N15 — Planned value is what the baseline said would be finished by today  ✅ PASS

**Design intent.** PV is the spend curve evaluated at today. It answers "where should we be", and it is the
only one of the three that knows about dates.

**Expected.** `10500`

**Derived from.** Today is months after this plan finished, so the whole baselined envelope is due.

**Actual.** `10500`

### N16 — Earned value is the budgeted worth of the work actually finished  ✅ PASS

**Design intent.** EV prices completed work at what it was BUDGETED, not what it cost. That is what lets AC
- EV isolate the overrun from the timing.

**Expected.** `6500`

**Derived from.** A 3,000 x 100% + B 2,500 x 100% + C 1,000 x 100% + D 4,000 x 0%

**Actual.** `6500`

### N17 — Actual cost is what has been booked  ✅ PASS

**Design intent.** AC is money out of the door. It carries no opinion about whether the work was worth it.

**Expected.** `7000`

**Derived from.** 3,000 + 3,500 + 500 + 0

**Actual.** `7000`

### N18 — The two measures point in OPPOSITE directions, and both are reported  ✅ PASS

**Design intent.** THE CASE THIS PLAN WAS BUILT FOR. Activity D is budgeted at $4,000 and never started, so
the project sits $3,500 under the spend curve while the work that DID finish came in
$500 over its budget. A panel reporting only the first number calls a cost overrun a
saving. Both must be available and the difference must be explainable.

**Expected.** `cv` = -500 · `svMoney` = -4000 · `gap` = -3500

**Derived from.** CV = EV 6,500 - AC 7,000 = -500 (over on work done). SV = EV 6,500 - PV 10,500 = -4,000 (behind). gap = AC 7,000 - PV 10,500 = -3,500 (under the curve).

**Actual.** `cv` = -500 · `svMoney` = -4000 · `gap` = -3500

### N19 — The three quantities reconcile: the gap is timing plus overrun  ✅ PASS

**Design intent.** gap = (EV - PV) + (AC - EV) is an identity, not a coincidence. It is the identity the
budget drill-in was rebuilt around, because without it a row cannot be split into the
half that costs nothing and the half that does.

**Expected.** `true`

**Derived from.** (-4,000) + (+500) = -3,500. Timing plus overrun equals the gap.

**Actual.** `true`

## Resourcing

### N20 — A conflict where all the overlapping work is finished is not counted  ✅ PASS

**Design intent.** DELIBERATE. Bob genuinely stands at 200% on 5 and 6 March, so a first-principles reading
says two over-allocated resource-days — and that is what an independent derivation
produced. The app reports zero because both activities are complete: you cannot
un-double-book a week that has already happened, and a finding you cannot act on trains
people to ignore the ones they can.

**Expected.** `0`

**Derived from.** B and C are both 100% complete, so the overlap is history.

**Actual.** `0`

### N21 — The same overlap IS counted while the work is still in flight  ✅ PASS

**Design intent.** The counterpart to N20, and the reason N20 is a rule rather than a bug. Counted as
(person x day) PAIRS: one person over on two days is two.

**Expected.** `days` = 2 · `people` = ["Bob"] · `dates` = ["2026-03-05","2026-03-06"]

**Derived from.** Set C back to 0% and Bob is over on Thu 5 and Fri 6 March: 2 pairs, 1 person, 2 dates.

**Actual.** `days` = 2 · `people` = ["Bob"] · `dates` = ["2026-03-05","2026-03-06"]

## RAID · cause vs watch

### B1 — An assumption you are relying on is never shown as the cause of a variance  ✅ PASS

**Design intent.** A RAID log holds two different kinds of statement. An Issue says "this went wrong" — it
is a cause. An Assumption says "we are counting on this" — it explains nothing until the
day it breaks. Showing the second beside a cost overrun in red tells the reader the
overrun has been explained when nobody has explained anything.

**Expected.** `causeChip` = false · `watchChip` = true · `watchIsRed` = false · `asksTheQuestion` = true

**Derived from.** Link an open Assumption with no recorded outcome to an activity and read the chips.

**Actual.** `causeChip` = false · `watchChip` = true · `watchIsRed` = false · `asksTheQuestion` = true

### B2 — A watched entry never suppresses the offer to record the real cause  ✅ PASS

**Design intent.** Every surface reads "show the cause, or offer to log one". While a watch entry counted
as a cause, the one activity you knew most about was the one activity you could no
longer explain — the offer disappeared because something irrelevant was linked to it.

**Expected.** `offersRaidWithAssumptionLinked` = true · `offersRaidWithNothingLinked` = true

**Derived from.** Compare the chip group with an assumption linked against nothing linked.

**Actual.** `offersRaidWithAssumptionLinked` = true · `offersRaidWithNothingLinked` = true

### B3 — Closing an issue does not erase the explanation it gave  ✅ PASS

**Design intent.** A cause does not stop being the cause once you have dealt with it. Filtering the chip to
open entries meant ticking Closed turned an explained overrun back into an unexplained
one, months later, with nobody able to say why.

**Expected.** `whileOpen` = true · `onceClosed` = true · `saysItIsClosed` = true

**Derived from.** Link an Issue, read the chip, set it Closed, read again.

**Actual.** `whileOpen` = true · `onceClosed` = true · `saysItIsClosed` = true

## RAID · outcomes

### B4 — A risk that materialised becomes a cause and reads as a fault  ✅ PASS

**Design intent.** Status (Open / Mitigating / Closed) tracks how much attention an entry needs, not how it
turned out — "Closed" on a risk reads the same whether it never happened or happened and
was handled. The outcome is a separate fact, and only an entry that turned may explain a
number.

**Expected.** `pendingIsWatch` = true · `pendingIsNotCause` = true · `materialisedIsCause` = true · `materialisedIsNotWatch` = true · `saysRiskHit` = true

**Derived from.** Move one Risk through pending and materialised, reading both chips at each step.

**Actual.** `pendingIsWatch` = true · `pendingIsNotCause` = true · `materialisedIsCause` = true · `materialisedIsNotWatch` = true · `saysRiskHit` = true

### B5 — A decision explains the work without reading as a fault  ✅ PASS

**Design intent.** A recorded decision genuinely explains why work cost what it did — but it is not a
problem, and it was written down before the variance existed. So it earns a chip in its
own neutral weight, and unlike a fault it does NOT stand in for the missing account of
why this particular number moved.

**Expected.** `getsAChip` = true · `notRed` = true · `noWarningGlyph` = true · `stillOffersRaid` = true

**Derived from.** Link a closed Decision to an activity and read the chip group.

**Actual.** `getsAChip` = true · `notRed` = true · `noWarningGlyph` = true · `stillOffersRaid` = true

### B6 — A turned entry offers to raise the issue it became, and writes nothing  ✅ PASS

**Design intent.** A risk that materialised is no longer a risk; it is a live problem. The honest move is
to open the Issue it became rather than quietly reinterpret the risk row as one —
prefilled and linked back to the source and forward to everything the source touched,
because those are exactly the activities now carrying it.

**Expected.** `type` = "Issue" · `linksBackToSource` = true · `carriesTheActivities` = true · `probabilityIsCertainty` = true · `wroteNothingToTheLog` = true

**Derived from.** Call raidRaiseFrom on a broken assumption and inspect the form, not the register.

**Actual.** `type` = "Issue" · `linksBackToSource` = true · `carriesTheActivities` = true · `probabilityIsCertainty` = true · `wroteNothingToTheLog` = true

## Budget drill-in

### B7 — A row's colour follows the overrun, never the total and never the critical path  ✅ PASS

**Design intent.** The figure on a driver row is booked minus what was due by today — the right number to
explain the bar, and NOT a verdict, because work happening early inflates it while
costing nothing. Colour must come from the half that is a fault. It has been wrong
twice: once from the total, once from .ptr-drv-crit repainting an under-budget row red
for a fact about the schedule.

**Expected.** `earlyAndUnderIsNotRed` = true · `genuinelyOverIsRed` = true · `colourFollowsOverrun` = true

**Derived from.** Build both states on the real export and read getComputedStyle, not the class list.

**Actual.** `earlyAndUnderIsNotRed` = true · `genuinelyOverIsRed` = true · `colourFollowsOverrun` = true

### B8 — The two halves of a row add back to the figure shown  ✅ PASS

**Design intent.** gap = timing + overrun is what makes the split honest. If the halves did not reconcile
with the total the row would be three unrelated numbers on one line.

**Expected.** `identityHolds` = true · `namesEveryMaterialHalf` = true · `inventsNoImmaterialHalf` = true

**Derived from.** Read every driver row, add its parts, and check the sentence against the parts. On the QA plan D Test is pure timing (-$4,000 behind its dates, no overrun) and C Docs is pure overrun (-$500 under its own budget, on its dates) — so a row must name the halves it HAS and stay silent about the ones it does not. An earlier version of this case demanded both halves always, which would have forced the panel to print "$0 ahead of its dates". The case was wrong, not the panel.

**Actual.** `identityHolds` = true · `namesEveryMaterialHalf` = true · `inventsNoImmaterialHalf` = true

### B9 — The list says truthfully whether its rows account for the whole bar  ✅ PASS

**Design intent.** The list is capped at five and one-sided, so a single row can be LARGER than the total
it sits under. Asserting "these add up to it" was false whenever any activity sat on the
other side.

**Expected.** `claimMatchesReality` = true

**Derived from.** Compare the note's claim against the actual remainder.

**Actual.** `claimMatchesReality` = true

## Plan vs actual

### B10 — A card's pair and its delta are measured against the same thing  ✅ PASS

**Design intent.** Every card reads "committed → actual" with a delta underneath. The Budget card compared
the whole envelope against spend so far, then printed a delta measured against what was
due today: three correct figures saying two unrelated things, so a reader gets "about
half the money gone" and then a number that seems to contradict it.

**Expected.** `pairAndDeltaReconcile` = true

**Derived from.** Parse the card and check the delta is recoverable from the pair above it.

**Actual.** `pairAndDeltaReconcile` = true

### B11 — A held end date still reports the movement underneath it  ✅ PASS

**Design intent.** Both ends of the schedule comparison are a max over the whole plan, so it only moves
when the LAST activity moves. "On the baseline date" then reads as "nothing has
happened" on a plan where four activities sat 81 activity-days off their own baselines.

**Expected.** `cardNamesTheDisplacement` = true · `agreesWithTheBar` = true

**Derived from.** Load the real export, where the end date holds and four activities have moved.

**Actual.** `cardNamesTheDisplacement` = true · `agreesWithTheBar` = true

## Client-safe mode

### B12 — Screen-share mode withholds every money figure from the report  ✅ PASS

**Design intent.** This is the control you turn on before sharing your screen with the client. One leaked
margin is the whole point of the feature failing.

**Expected.** `moneyFiguresWhenOn` = 0 · `moneyFiguresWhenOff` = true · `reportStillUsable` = true

**Derived from.** Build the status report with the toggle on and off and count currency figures.

**Actual.** `moneyFiguresWhenOn` = 0 · `moneyFiguresWhenOff` = true · `reportStillUsable` = true

### B13 — Money is withheld before the request body is built, not after  ✅ PASS

**Design intent.** The AI digest is what actually leaves the browser. If screen-share mode bit only at
render, the money would already be in the request by the time anyone noticed.

**Expected.** `moneyInSafeDigest` = 0 · `moneyInOpenDigest` = true

**Derived from.** Build ptDigest with the toggle on and off.

**Actual.** `moneyInSafeDigest` = 0 · `moneyInOpenDigest` = true

## Status report

### B14 — The report states what the work COST, not only what it was worth  ✅ PASS

**Design intent.** Earned value is what the finished work was worth; it is not what it cost, and the
difference between them is the entire over-or-under question. A status report that
quotes only the first asks the client to take the good news on trust.

**Expected.** `statesBooked` = true · `statesEarned` = true · `statesTheDifference` = true

**Derived from.** Read the cost line of the generated report.

**Actual.** `statesBooked` = true · `statesEarned` = true · `statesTheDifference` = true

## AI boundary

### B15 — Model output that cannot resolve is dropped, and the reason is named  ✅ PASS

**Design intent.** The model may only reference entities from a catalogue this file builds. What protects
the user is not the model behaving but the validator refusing — and refusing OUT LOUD,
because a silent drop is indistinguishable from the model having said nothing.

**Expected.** `inventedPanelDropped` = true · `fabricatedCitationDropped` = true · `hallucinatedRefsDropped` = true · `everyDropNamed` = true

**Derived from.** Hand-write four hallucinated responses and put them through ptValidate.

**Actual.** `inventedPanelDropped` = true · `fabricatedCitationDropped` = true · `hallucinatedRefsDropped` = true · `everyDropNamed` = true

### B16 — An injected sentence is kept as text and is inert when drawn  ✅ PASS

**Design intent.** DELIBERATE. Dropping text for containing frightening words is keyword filtering and
fails on the first phrasing nobody anticipated. The real protection is that a reading
carries no execution power and is escaped on render — so the escaping is what gets
tested, not a blocklist.

**Expected.** `keptAsText` = true · `inertWhenDrawn` = true

**Derived from.** Put a script tag through the validator and render the result.

**Actual.** `keptAsText` = true · `inertWhenDrawn` = true

## Schedule presentation

### B17 — The panel explains why the finish is a day beyond the duration  ✅ PASS

**Design intent.** The counterpart to N10. The milestone convention is a defensible choice, but "12.0 days
-> finish Mar 18" on a plan starting Monday 2 March cannot be read naively — count the
working days and you reach the thirteenth. Making the choice silently is what turned a
convention into an apparent bug for three independent readers.

**Expected.** `spanIsOneLonger` = true · `panelNamesTheMilestoneDay` = true

**Derived from.** Load the QA reference plan and read the CPM line.

**Actual.** `spanIsOneLonger` = true · `panelNamesTheMilestoneDay` = true

### B18 — A committed date reports the confidence it actually carries  ✅ PASS

**Design intent.** The simulation produces DURATIONS, which are continuous; a committed date is a whole
working day and rounds up. So a date sized from the P80 absorbs slightly more of the
distribution than 80% — conservative, never the reverse, but sizing contingency for 80%
while buying 82.8% means paying the difference in quoted duration.

**Expected.** `reportsADateConfidence` = true · `atLeastTheNominal` = true · `notTheNominalItself` = true

**Derived from.** Load the real export, run the simulation, size from P80 and read back.

**Actual.** `reportsADateConfidence` = true · `atLeastTheNominal` = true · `notTheNominalItself` = true

## Resourcing presentation

### B19 — The over-allocation count is reported in the unit it is counted in  ✅ PASS

**Design intent.** computeResourceLoad counts (person x day) PAIRS. Every surface calls that a resource-day
except the line printed right after clicking Level, which said "Over-allocated days" — a
different quantity the moment two people are over on the same date, and one that reads
as consecutive calendar days of a single person.

**Expected.** `namesTheUnit` = true · `doesNotSayBareDays` = true · `tabStatesTheTotal` = true

**Derived from.** Force a conflict on the real export, level it, and read both the status line and the tab.

**Actual.** `namesTheUnit` = true · `doesNotSayBareDays` = true · `tabStatesTheTotal` = true

## Persistence

### B20 — A recorded outcome survives save and reload, and an old file loads as unanswered  ✅ PASS

**Design intent.** serialize() lists task fields explicitly and hydrate() maps them explicitly, so a field
added to one and not the other is written on save and lost on load — this has happened
before. And a file written before outcomes existed must load as "not recorded", not as
something that turned.

**Expected.** `survivesRoundTrip` = true · `legacyFileLoadsUnanswered` = true

**Derived from.** Save an entry with an outcome, reload it, then strip the field and reload again.

**Actual.** `survivesRoundTrip` = true · `legacyFileLoadsUnanswered` = true

## Data integrity

### B21 — Drafting an entry from a variance fills the form and writes nothing  ✅ PASS

**Design intent.** An entry nobody typed is an entry nobody believes, and the RAID log is the one place in
this file a client reads word for word. Every AI and shortcut path must stop at the
form.

**Expected.** `formPrefilled` = true · `linkedToTheActivity` = true · `registerUntouched` = true

**Derived from.** Call the draft path and compare the register length before and after.

**Actual.** `formPrefilled` = true · `linkedToTheActivity` = true · `registerUntouched` = true

## Test plan

### B24 — No test case waits on another test case, in the sample or after a repair  ✅ PASS

**Design intent.** A test case verifies an activity. Chaining one case to the next puts the whole pack on a
serial line: the finish date carries the SUM of every case rather than the longest, one
failure blocks everything behind it, and — because a chain can reach across story groups
to a case whose own activity runs later — it is where the schedule loops come from. The
sample used to ship forty cases in one chain, which meant the demo everyone opens first
showed the defect and the warning banner about it. loadSample now runs the same repair
the app offers, so the rule is proved on the data it ships with. Nothing checked that,
and a silent removal of that one line would put the chain back on the public demo.

**Expected.** `sampleShipsChained` = 0 · `sampleHasCases` = true · `sampleHasNoLoop` = true · `repairClearsAChain` = 0 · `repairPointsAtTheActivityNotACase` = true

**Derived from.** Load the sample and count. Then chain three cases by hand, repair, and count again.

**Actual.** `sampleShipsChained` = 0 · `sampleHasCases` = true · `sampleHasNoLoop` = true · `repairClearsAChain` = 0 · `repairPointsAtTheActivityNotACase` = true

